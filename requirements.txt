# The desktop application — the primary way to use AlgaMetrix from source.
#
# Three files, because three different jobs need three different sets:
#   requirements.txt        this one: run the desktop app
#   requirements-paper.txt  reproduce the paper (reproduce.py) — needs SciPy
#   requirements-dev.txt    run the test suite and build the executable
#
# The optional Streamlit web client is not here: pip install ".[web]".

# Engine
numpy>=1.26
pandas>=2.1
PyYAML>=6.0

# Desktop client
PySide6>=6.6
matplotlib>=3.8
openpyxl>=3.1

# On a minimal Linux image Qt also needs system libraries pip cannot install:
#   sudo apt-get install libegl1 libgl1 libxkbcommon-x11-0 libdbus-1-3 libxcb-cursor0
